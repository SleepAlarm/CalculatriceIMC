import 'package:flutter/material.dart';
class Result_page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('CALCULATRICE IMC'),
      ),
      body: Text('Voici les resultats'),
    );
  }
}
